export type SummaryData = {
  balance: number;
  incomeMonth: number;
  expenseMonth: number;
};

export type CategoryData = { category: string; total: number };

export type AnalyticData = { income: number; expense: number; date: string };
